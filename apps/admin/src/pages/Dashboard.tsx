import{useEffect,useState}from'react';import type{ApiClient}from'../api';type Data={members:number;events:number;attendance:number;giving:Record<string,number>;engagement:{posts:number;reactions:number}};
export function Dashboard({api}:{api:ApiClient}){const[data,setData]=useState<Data|null>(null);useEffect(()=>{api.request<Data>('reports').then(setData).catch(()=>setData(null));},[]);const cards=[['Active members',data?.members??'—','+ engagement'],['Events',data?.events??'—','this period'],['Attendance',data?.attendance??'—','check-ins'],['Community posts',data?.engagement.posts??'—','published']];return <><section className="hero"><div><span className="eyebrow gold">MINISTRY PULSE</span><h2>Your church at a glance</h2><p>Live operational insight across people, services, generosity and community.</p></div><button>Download report</button></section><section className="metrics">{cards.map(([label,value,detail])=><article key={label}><span>{label}</span><strong>{value}</strong><small>{detail}</small></article>)}</section><section className="grid"><article className="panel wide"><div className="panel-title"><div><h3>Engagement trend</h3><p>Attendance and community activity</p></div><select><option>Last 30 days</option></select></div><div className="chart"><div/><div/><div/><div/><div/><div/><div/></div></article><article className="panel"><h3>Giving</h3><p>Successful donations by currency</p>{Object.entries(data?.giving??{}).map(([currency,total])=><div className="giving-row" key={currency}><span>{currency}</span><strong>{(total/100).toLocaleString()}</strong></div>)}{!Object.keys(data?.giving??{}).length&&<div className="empty">No giving data yet</div>}</article></section></>}
