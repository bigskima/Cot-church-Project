export interface AuthState{accessToken:string;organizationId?:string;branchId?:string}const key='church-admin-auth';export function loadAuth(){const value=sessionStorage.getItem(key);return value?JSON.parse(value)as AuthState:null}export function saveAuth(value:AuthState|null){if(value)sessionStorage.setItem(key,JSON.stringify(value));else sessionStorage.removeItem(key)}
export class ApiClient{constructor(private getAuth:()=>AuthState|null){}async request<T>(path:string,init:RequestInit={}){const auth=this.getAuth(),response=await fetch(`${import.meta.env.VITE_API_URL}/${path}`,{...init,headers:{Accept:'application/json',...(init.body?{'Content-Type':'application/json'}:{}),...(auth?.accessToken?{Authorization:`Bearer ${auth.accessToken}`}:{ }),...(auth?.organizationId?{'X-Organization-Id':auth.organizationId}:{}),...(auth?.branchId?{'X-Branch-Id':auth.branchId}:{}),...init.headers}}),payload=await response.json();if(!response.ok)throw new Error(payload.error?.message??'Request failed');return payload.data as T}}
