import{useState,type FormEvent}from'react';import type{ApiClient,AuthState}from'../api';
export function Login({api,onAuthenticated}:{api:ApiClient;onAuthenticated:(auth:AuthState)=>void}){const[email,setEmail]=useState(''),[password,setPassword]=useState(''),[error,setError]=useState(''),[busy,setBusy]=useState(false);async function submit(event:FormEvent){event.preventDefault();setBusy(true);setError('');try{const data=await api.request<{session:{accessToken:string}}>('login',{method:'POST',body:JSON.stringify({email,password})});onAuthenticated({accessToken:data.session.accessToken})}catch(value){setError(value instanceof Error?value.message:'Unable to sign in')}finally{setBusy(false)}}return <main className="login"><section className="login-art"><span className="eyebrow">GLOBAL CHURCH PLATFORM</span><h1>Lead with clarity.<br/>Serve with care.</h1><p>One secure operating system for people, ministry, generosity and engagement.</p></section><form className="login-card" onSubmit={submit}><div className="mark">C</div><h2>Admin sign in</h2><p>Use your authorized leadership account.</p><label>Email<input type="email" autoComplete="email" value={email} onChange={event=>setEmail(event.target.value)} required/></label><label>Password<input type="password" autoComplete="current-password" value={password} onChange={event=>setPassword(event.target.value)} required/></label>{error&&<div className="error">{error}</div>}<button disabled={busy}>{busy?'Signing in…':'Sign in securely'}</button></form></main>}
