import "jsr:@supabase/functions-js/edge-runtime.d.ts";
import{ApiError}from"../_shared/errors.ts";import{createHandler}from"../_shared/handler.ts";import{publicClient}from"../_shared/supabase.ts";import{uuid}from"../_shared/validation.ts";
Deno.serve(createHandler({methods:["GET"],authentication:"none",organization:"none"},async({request})=>{const url=new URL(request.url),organizationId=uuid(url.searchParams.get("organizationId"),"organizationId",true)!,type=url.searchParams.get("type")??"feed";const client=publicClient();if(type==="streams"){const{data,error}=await client.from("live_streams").select("id,title,description,status,playback_url,playback_token_required,scheduled_start,started_at,ended_at,recording_url,thumbnail_url").eq("organization_id",organizationId).eq("visibility","public").in("status",["scheduled","live","ended"]).order("scheduled_start",{ascending:false}).limit(50);if(error)throw new ApiError("PUBLIC_STREAMS_FAILED","Unable to retrieve public streams",500,undefined,false);return{data:(data??[]).map(stream=>({...stream,playback_url:stream.playback_token_required?null:stream.playback_url,recording_url:stream.playback_token_required?null:stream.recording_url}))};}const{data,error}=await client.from("social_posts").select("id,body,media,published_at").eq("organization_id",organizationId).eq("visibility","public").eq("status","published").order("published_at",{ascending:false}).limit(50);if(error)throw new ApiError("PUBLIC_FEED_FAILED","Unable to retrieve public feed",500,undefined,false);return{data:data??[]};}));
