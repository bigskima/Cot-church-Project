import "jsr:@supabase/functions-js/edge-runtime.d.ts";
import{getNotificationWorkerSecret}from"../_shared/config.ts";import{ApiError}from"../_shared/errors.ts";import{createHandler}from"../_shared/handler.ts";import{jsonBody}from"../_shared/request.ts";import{adminClient}from"../_shared/supabase.ts";import{assertNoUnknownFields,assertObject,requiredString}from"../_shared/validation.ts";
function secureEqual(left:string,right:string){const a=new TextEncoder().encode(left),b=new TextEncoder().encode(right);let result=a.length^b.length;for(let i=0;i<Math.max(a.length,b.length);i++)result|=(a[i%a.length]??0)^(b[i%b.length]??0);return result===0;}
Deno.serve(createHandler({methods:["POST"],authentication:"none",organization:"none"},async({request})=>{const supplied=request.headers.get("x-worker-secret")??"";if(!supplied||!secureEqual(supplied,getNotificationWorkerSecret()))throw new ApiError("AUTHENTICATION_REQUIRED","Worker authentication required",401);const body=assertObject(await jsonBody(request));const action=requiredString(body.action,"action",20);const client=adminClient();if(action==="claim"){assertNoUnknownFields(body,["action","batchSize"]);const size=body.batchSize===undefined?50:Number(body.batchSize);if(!Number.isInteger(size)||size<1||size>100)throw new ApiError("VALIDATION_FAILED","batchSize must be 1-100",422);const{data,error}=await client.rpc("claim_notification_outbox",{batch_size:size});if(error)throw new ApiError("OUTBOX_CLAIM_FAILED","Unable to claim notification jobs",500,undefined,false);return{data:data??[]};}assertNoUnknownFields(body,["action","jobId","provider","providerMessageId","error"]);if(!["delivered","failed"].includes(action))throw new ApiError("VALIDATION_FAILED","Invalid dispatch action",422);const jobId=Number(body.jobId);if(!Number.isInteger(jobId)||jobId<1)throw new ApiError("VALIDATION_FAILED","Invalid jobId",422);const status=action==="delivered"?"delivered":"failed";const updates=action==="delivered"?{status,delivered_at:new Date().toISOString(),locked_at:null,last_error:null}:{status,locked_at:null,last_error:requiredString(body.error,"error",1000),available_at:new Date(Date.now()+60000).toISOString()};const{error}=await client.from("notification_outbox").update(updates).eq("id",jobId).eq("status","processing");if(error)throw new ApiError("OUTBOX_ACK_FAILED","Unable to acknowledge notification job",500,undefined,false);await client.from("notification_deliveries").insert({outbox_id:jobId,provider:requiredString(body.provider,"provider",80),provider_message_id:body.providerMessageId??null,status,response_metadata:{}});return{data:{status}};}));
