import "jsr:@supabase/functions-js/edge-runtime.d.ts";
import{ApiError}from"../_shared/errors.ts";import{createHandler}from"../_shared/handler.ts";import{jsonBody}from"../_shared/request.ts";import{assertNoUnknownFields,assertObject,requiredString,uuid}from"../_shared/validation.ts";
Deno.serve(createHandler({methods:["GET","PATCH"],authentication:"required",organization:"required"},async({request,auth})=>{if(!auth?.organizationId)throw new ApiError("ORGANIZATION_REQUIRED","Organization context is required",400);if(request.method==="GET"){const url=new URL(request.url);let query=auth.client.from("notifications").select("id,type,title,body,data,read_at,created_at").eq("organization_id",auth.organizationId).eq("recipient_profile_id",auth.user.id).order("created_at",{ascending:false}).limit(100);if(url.searchParams.get("unread")==="true")query=query.is("read_at",null);const{data,error}=await query;if(error)throw new ApiError("NOTIFICATION_LIST_FAILED","Unable to retrieve notifications",500,undefined,false);return{data:data??[]};}const body=assertObject(await jsonBody(request));assertNoUnknownFields(body,["id","read"]);if(typeof body.read!=="boolean")throw new ApiError("VALIDATION_FAILED","read must be boolean",422);const{data,error}=await auth.client.from("notifications").update({read_at:body.read?new Date().toISOString():null}).eq("id",uuid(requiredString(body.id,"id",36),"id",true)).eq("recipient_profile_id",auth.user.id).select("id,read_at").single();if(error)throw new ApiError("NOTIFICATION_UPDATE_FAILED","Unable to update notification",500,undefined,false);return{data};}));
