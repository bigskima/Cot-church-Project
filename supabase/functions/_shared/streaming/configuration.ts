import{ApiError}from'../errors.ts';import{adminClient}from'../supabase.ts';import type{ProviderConfiguration}from'./types.ts';export type LoadedStreamingConfig={id:string;organizationId:string|null;providerId:string;provider:ProviderConfiguration};
export async function loadStreamingConfig(configId:string){const{data,error}=await adminClient().from('streaming_provider_configs').select('id,organization_id,secret_reference,webhook_secret_reference,signing_key_reference,configuration,streaming_providers!inner(id,code,is_active)').eq('id',configId).eq('is_active',true).single();if(error||!data)throw new ApiError('STREAMING_NOT_CONFIGURED','Streaming provider configuration is unavailable',503,undefined,false);const relation=data.streaming_providers as unknown as{id:string;code:string;is_active:boolean};if(!relation.is_active)throw new ApiError('STREAMING_PROVIDER_DISABLED','Streaming provider is disabled',503);return{id:data.id,organizationId:data.organization_id,providerId:relation.id,provider:{providerCode:relation.code,secretReference:data.secret_reference,webhookSecretReference:data.webhook_secret_reference,signingKeyReference:data.signing_key_reference,settings:data.configuration as Record<string,unknown>}} as LoadedStreamingConfig}
export async function defaultStreamingConfig(organizationId:string){const client=adminClient();let query=client.from('streaming_provider_configs').select('id').eq('is_active',true).eq('is_default',true).or(`organization_id.eq.${organizationId},organization_id.is.null`).order('organization_id',{ascending:false}).limit(1).maybeSingle();const{data,error}=await query;if(error||!data)throw new ApiError('STREAMING_NOT_CONFIGURED','No active streaming provider is configured',503);return loadStreamingConfig(data.id)}
