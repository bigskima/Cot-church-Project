import { ApiError } from './errors.ts';
export function secretValue(reference:string){if(!/^[A-Z][A-Z0-9_]{2,127}$/.test(reference))throw new ApiError('INVALID_SECRET_REFERENCE','Server secret reference is invalid',500,undefined,false);const value=Deno.env.get(reference);if(!value)throw new ApiError('SECRET_UNAVAILABLE','Required server secret is unavailable',500,undefined,false);return value}
export function secretJson<T>(reference:string):T{const raw=secretValue(reference);let value:unknown;try{value=JSON.parse(raw)}catch{throw new ApiError('SECRET_INVALID','Server secret is not valid JSON',500,undefined,false)}if(!value||typeof value!=='object'||Array.isArray(value))throw new ApiError('SECRET_INVALID','Server secret has an invalid shape',500,undefined,false);return value as T}
