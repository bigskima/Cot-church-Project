import "jsr:@supabase/functions-js/edge-runtime.d.ts";import{ApiError}from"../_shared/errors.ts";import{authorize}from"../_shared/context.ts";import{createHandler}from"../_shared/handler.ts";import{jsonBody}from"../_shared/request.ts";import{runAi}from"../_shared/ai/router.ts";import{assertNoUnknownFields,assertObject,optionalString,requiredString,uuid}from"../_shared/validation.ts";
const allowed=new Set(['assistant.answer','sermon.summarize','translate.text','content.moderate','pastoral.triage','admin.insight']);
Deno.serve(createHandler({methods:['POST'],authentication:'required',organization:'required'},async({request,auth})=>{if(!auth?.organizationId)throw new ApiError('ORGANIZATION_REQUIRED','Organization context is required',400);await authorize(auth,'ai.use');const body=assertObject(await jsonBody(request));assertNoUnknownFields(body,['capability','prompt','language','entityType','entityId']);const capability=requiredString(body.capability,'capability',60);if(!allowed.has(capability))throw new ApiError('AI_CAPABILITY_DENIED','Capability is not available through this endpoint',422);const prompt=requiredString(body.prompt,'prompt',12000),entityType=optionalString(body.entityType,'entityType',50),entityId=body.entityId?uuid(String(body.entityId),'entityId',true):undefined;let verifiedContext='';if(capability==='assistant.answer'){const[branches,events,announcements]=await Promise.all([auth.client.from('branches').select('name,timezone,address').eq('organization_id',auth.organizationId).eq('is_active',true).limit(30),auth.client.from('events').select('title,starts_at,ends_at,location,visibility').eq('organization_id',auth.organizationId).gte('ends_at',new Date().toISOString()).limit(30),auth.client.from('announcements').select('title,body,published_at').eq('organization_id',auth.organizationId).eq('status','published').limit(20)]);verifiedContext=JSON.stringify({branches:branches.data??[],events:events.data??[],announcements:announcements.data??[]})}const system=`You are the church platform assistant. Use only verified tenant-scoped context. Never invent people, times, policies or pastoral claims. Never reveal private prayer, counselling, giving or attendance records. If uncertain, say so. Verified context: ${verifiedContext}`;const result=await runAi({organizationId:auth.organizationId,profileId:auth.user.id,capabilityCode:capability,request:{model:'resolved-by-route',system,prompt,language:optionalString(body.language,'language',30),jsonSchema:capability==='pastoral.triage'?{type:'object',properties:{category:{type:'string'},urgency:{type:'string'},suggestedWorkflow:{type:'string'},requiresHumanReview:{type:'boolean'}},required:['category','urgency','requiresHumanReview']}:undefined},entityType,entityId});return{data:result,status:result.status==='requires_review'?202:200};}));
