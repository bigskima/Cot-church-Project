import "jsr:@supabase/functions-js/edge-runtime.d.ts";
import { ApiError } from "../_shared/errors.ts";import{authorize}from"../_shared/context.ts";import{createHandler}from"../_shared/handler.ts";import{jsonBody}from"../_shared/request.ts";import{assertNoUnknownFields,assertObject,optionalString,requiredString,uuid}from"../_shared/validation.ts";
const channels=new Set(["in_app","email","sms","push"]);
Deno.serve(createHandler({methods:["GET","POST","PATCH"],authentication:"required",organization:"required"},async({request,auth})=>{if(!auth?.organizationId)throw new ApiError("ORGANIZATION_REQUIRED","Organization context is required",400);if(request.method==="GET"){const{data,error}=await auth.client.from("announcements").select("id,branch_id,title,body,status,audience,channels,scheduled_for,published_at,created_at,updated_at").eq("organization_id",auth.organizationId).order("created_at",{ascending:false}).limit(100);if(error)throw new ApiError("ANNOUNCEMENT_LIST_FAILED","Unable to retrieve announcements",500,undefined,false);return{data:data??[]};}
 const body=assertObject(await jsonBody(request));if(body.action==="publish"){assertNoUnknownFields(body,["action","id"]);const{data,error}=await auth.client.rpc("publish_announcement",{target_announcement_id:uuid(requiredString(body.id,"id",36),"id",true)}).single();if(error?.code==="42501")throw new ApiError("PERMISSION_DENIED","Permission denied",403);if(error)throw new ApiError("ANNOUNCEMENT_PUBLISH_FAILED","Unable to publish announcement",500,undefined,false);return{data};}
 await authorize(auth,"announcements.manage");assertNoUnknownFields(body,["id","branchId","title","body","audience","channels","scheduledFor","status"]);const record:Record<string,unknown>={};if(request.method==="POST"||body.title!==undefined)record.title=requiredString(body.title,"title",180);if(request.method==="POST"||body.body!==undefined)record.body=requiredString(body.body,"body",20000);if(body.branchId!==undefined)record.branch_id=body.branchId===null?null:uuid(String(body.branchId),"branchId",true);if(body.audience!==undefined){if(!body.audience||typeof body.audience!=="object"||Array.isArray(body.audience))throw new ApiError("VALIDATION_FAILED","audience must be an object",422);record.audience=body.audience;}if(body.channels!==undefined){if(!Array.isArray(body.channels)||body.channels.some(channel=>typeof channel!=="string"||!channels.has(channel)))throw new ApiError("VALIDATION_FAILED","Invalid channels",422);record.channels=body.channels;}if(body.scheduledFor!==undefined)record.scheduled_for=body.scheduledFor;if(body.status!==undefined)record.status=optionalString(body.status,"status",20);
 if(request.method==="POST"){Object.assign(record,{organization_id:auth.organizationId,created_by:auth.user.id});const{data,error}=await auth.client.from("announcements").insert(record).select().single();if(error)throw new ApiError("ANNOUNCEMENT_CREATE_FAILED","Unable to create announcement",500,undefined,false);return{data,status:201};}const id=uuid(requiredString(body.id,"id",36),"id",true)!;const{data,error}=await auth.client.from("announcements").update(record).eq("id",id).eq("organization_id",auth.organizationId).select().single();if(error)throw new ApiError("ANNOUNCEMENT_UPDATE_FAILED","Unable to update announcement",500,undefined,false);return{data};}));
